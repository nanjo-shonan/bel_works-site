{% macro heroSection(title, subtitle, description) %}
{# --- ヒーローセクション --- #}
{# 備考: 左揃えを徹底し、CTAボタンを削除。次のセクションへの自然な視線誘導を促す #}
<div class="min-h-[60vh] flex flex-col justify-center">
    <h1 class="text-4xl sm:text-5xl font-bold font-sans mb-4 leading-tight text-blueprint-black">{{ title }}</h1>
    <p class="text-lg sm:text-xl text-gray-600">{{ subtitle }}<br>{{ description }}</p>
</div>
{% endmacro %}
