{#
  src/_includes/layouts/base.njk (修正後)
  -----------------------------------------
  [修正のポイント]
  1. ヘッダー({% include "partials/header.njk" %})を、
     スクロールアニメーションの対象である <div id="container"> の外側に移動しました。
  2. フッター({% include "partials/footer.njk" %})も同様に、
     <div id="container"> の外側に移動しました。

  これにより、#container に transform スタイルが適用されても、
  ヘッダーとフッターの position:fixed の挙動に影響がなくなります。
#}
<!DOCTYPE html>
<html lang="{{ metadata.language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
</head>
<body>

    <!-- ★ヘッダーをアニメーションコンテナの外に出す -->
    {% include "partials/header.njk" %}

    <!-- スクロールアニメーションを適用するコンテナ -->
    <div id="container" class="container">
        <main>
            <!-- 各ページのコンテンツがここに挿入される -->
            {{ content | safe }}
        </main>
    </div><!-- /#container -->

    <!-- ★フッターもアニメーションコンテナの外に出す -->
    {% include "partials/footer.njk" %}

    <!-- JavaScriptライブラリ -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.5/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.8/ScrollMagic.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/ScrollMagic/2.0.8/plugins/animation.gsap.min.js"></script>
    <script src="{{ '/assets/js/main.js' | url }}"></script>

</body>
</html>
